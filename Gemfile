source 'http://www.rubygems.org'

gem 'jekyll'

# Security patches for vulnerable dependencies
gem 'rexml', '>= 3.3.6'        # Fixes DoS and ReDoS vulnerabilities
gem 'webrick', '>= 1.8.2'       # Fixes HTTP Request Smuggling
gem 'google-protobuf', '>= 3.25.5' # Fixes DoS vulnerability